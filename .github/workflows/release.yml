name: Release
on:
  workflow_dispatch:
    inputs:
      base-branch:
        description: 'Which branch to create the release from (e.g., master, release-0.8.x)'
        default: 'main'
        required: false
env:
  NODE_VERSION: 14
  KEPTN_BOT_NAME: "Keptn Bot"
  KEPTN_BOT_EMAIL: "keptn-bot <86361500+keptn-bot@users.noreply.github.com>"
defaults:
  run:
    shell: bash
jobs:
  test:
    strategy:
      matrix:
        go-version: [1.16.x]
        platform: [ubuntu-latest]
    runs-on: ${{ matrix.platform }}
    env:
      GO111MODULE: "on"
      GOPROXY: "https://proxy.golang.org"
    steps:
      - name: Install Go
        uses: actions/setup-go@v2.1.3
        with:
          go-version: ${{ matrix.go-version }}
      - name: Checkout code
        uses: actions/checkout@v2.3.4
      - name: Test
        run: go test -race -v ./...
  release:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout repo
        uses: actions/checkout@v2
        with:
          ref: ${{ github.event.inputs.base-branch }}

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Set up semantic-release
        run: npm install conventional-changelog-conventionalcommits @semantic-release/commit-analyzer @semantic-release/release-notes-generator @semantic-release/github @semantic-release/changelog @semantic-release/git

      - name: Release to GitHub
        env:
          GITHUB_TOKEN: ${{ secrets.KEPTN_BOT_TOKEN }}
          GIT_AUTHOR_NAME: ${{ env.KEPTN_BOT_NAME }}
          GIT_AUTHOR_EMAIL: ${{ env.KEPTN_BOT_EMAIL }}
          GIT_COMMITTER_NAME: ${{ env.KEPTN_BOT_NAME }}
          GIT_COMMITTER_EMAIL: ${{ env.KEPTN_BOT_EMAIL }}
        run: npx semantic-release
